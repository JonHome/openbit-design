# `ui-domain-openbit`

OpenBit domain pack assets built on top of `ui-kernel` and `ui-foundation`.

- `presets/`: semantic business presets consumed by app pages
- `tokens/themes/`: domain-specific theme overlays

The pack stays domain-specific while the underlying component contracts remain product-agnostic.
